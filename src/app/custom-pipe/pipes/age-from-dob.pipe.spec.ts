import { AgeFromDobPipe } from './age-from-dob.pipe';

describe('AgeFromDobPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeFromDobPipe();
    expect(pipe).toBeTruthy();
  });
});
